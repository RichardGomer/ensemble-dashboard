ensemble web
============

A web interface for Ensemble IOT

## Connecting to Ensemble Mesh

The UI connects to ensemble using a websocket. You need to run the
proxy service with the websocket/run.php script inside ensemble itself.


## Run UI

First,

`npm install --save-dev parcel`

Then just

`npx parcel public/index.html`


